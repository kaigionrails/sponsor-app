- content_for(:title) { t('.title') }

%h2 Listing conferences

.row.py-5
  - @conferences.each do |conference|
    .col-md-6
      .card
        .card-body
          = link_to conference.name, conference

= link_to 'New', new_conference_path, class: 'btn btn-default'
